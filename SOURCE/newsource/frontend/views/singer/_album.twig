<div class="mdl-1 mdl-video-nominations">
              <div class="title ellipsis">
                  <a href="javascript:void(0)" class="txt">Albums</a>
              </div>
				<div class="row">
          {% for val in listAlbum %}
            <div class="col-xs-6 col-md-4 item">
                <a href="{{ ajax_url('playlist/detail',{'slug':val.slug})|raw }}" class="image-play">
                    <img width="100%" src="{{ val.image_path|imagePathThumb(116) }}">
                    <i class="fa icon-play"></i>
                </a>
                <a href="{{ ajax_url('playlist/detail',{'slug':val.slug})|raw }}" class="song-name ellipsis">
                  {{val.name}}</a>
            </div>
          {% endfor %}


                 </div>
		  </div>
